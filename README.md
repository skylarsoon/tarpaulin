# Tarpaulin
### A lightweight Flask REST API for a course management application similar to Canvas.
- Uses Google Datastore (for user information) and Google Cloud Storage (for avatars).
- Uses Auth0 and JWT verification to ensure valid access to endpoints.
